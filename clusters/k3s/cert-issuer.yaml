apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-issuer
  namespace: flux-system
spec:
  dependsOn:
    - name: cert-manager
  interval: 10m
  prune: true
  wait: true
  path: ./infrastructure/cert-issuer
  sourceRef:
    kind: GitRepository
    name: flux-system
