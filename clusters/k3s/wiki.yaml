apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: wiki
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/wiki
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: wiki
  decryption:
    provider: sops
    secretRef:
      name: sops-age