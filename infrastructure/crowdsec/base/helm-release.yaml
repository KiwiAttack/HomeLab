apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crowdsec
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: crowdsec
  releaseName: crowdsec
  chart:
    spec:
      chart: crowdsec
      version: 0.19.3
      sourceRef:
        kind: HelmRepository
        name: crowdsec
        namespace: flux-system
  values:
    agent:
      container_runtime: cri
      log_level: debug
      acquisition:
        - filenames:
          - /var/log/containers/*-ingress-nginx-controller-*_*_*.log
          force_inotify: true
          labels:
            type: containerd
            program: nginx
      tolerations:
        - key: "node-role.kubernetes.io/master"
          operator: "Exists"
          effect: "NoSchedule"
    lapi:
      resources:
        limits:
          memory: 100Mi
        requests:
          cpu: 150m
          memory: 100Mi
      persistentVolume:
        data:
          enabled: true
          accessModes:
            - ReadWriteOnce
          storageClassName: ""
          existingClaim: ""
          size: 1Gi
        config:
          enabled: true
          accessModes:
            - ReadWriteOnce
          storageClassName: ""
          existingClaim: "lapi-crowdsec-pvc"
          size: 1Gi