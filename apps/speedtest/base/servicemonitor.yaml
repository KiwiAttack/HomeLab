apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: speedtest-tracker
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: speedtest-tracker
  namespaceSelector:
    matchNames:
      - speedtest-tracker
  endpoints:
    - port: http
      path: /metrics
      interval: 60s